function [P] = kWh2price(E)
% E is the kWh per week consumed
% P is the price per kWh expected

% First estimate the cost associated solar panels kW installation required
kW_inst = kWh2kW (E);   % Estimates the power installation 



end

